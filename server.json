{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.mkpvishnu/terminal-mcp",
  "description": "MCP server for interactive terminal sessions \u2014 SSH, REPLs, database CLIs, TUI apps",
  "repository": {
    "url": "https://github.com/mkpvishnu/terminal-mcp",
    "source": "github"
  },
  "version": "0.4.6",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "terminal-mcp",
      "version": "0.4.6",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "description": "Maximum concurrent sessions",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "TERMINAL_MCP_MAX_SESSIONS"
        },
        {
          "description": "Seconds before auto-close (default 1800)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "TERMINAL_MCP_IDLE_TIMEOUT"
        },
        {
          "description": "Max per-session buffer bytes (default 1000000)",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "TERMINAL_MCP_MAX_BUFFER_BYTES"
        }
      ]
    }
  ]
}
